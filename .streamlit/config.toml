[server]
# 这个参数是关键。它设置了 WebSocket 连接的 ping 超时时间 (单位: 秒)。
# 默认值是 20。我们把它调大到 300 秒，以容忍长时间的计算。
websocketPingTimeout = 300

# (可选，但推荐) 确保端口号一致
port = 8501

# (可选) 如果你希望可以通过IP地址访问，取消下面这行的注释
# headless = true